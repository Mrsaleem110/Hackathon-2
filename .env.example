# Example environment variables for Dockerized AI Todo Chatbot

# FastAPI Backend Configuration
# Neon PostgreSQL DATABASE_URL. Format:
# postgresql://[user]:[password]@[host]:[port]/[database_name]?sslmode=require
DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require"

# Node.js Frontend Configuration
# API_URL should point to the backend service name and port within the Docker network
API_URL="http://backend:8000"
